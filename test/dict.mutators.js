// Generated by CoffeeScript 1.3.3
var Dictionary, TypedDictionary, expect, _ref;

expect = require('expect.js');

_ref = require('../lib/coll'), TypedDictionary = _ref.TypedDictionary, Dictionary = _ref.Dictionary;

describe('Dictionary Mutators', function() {
  var d1, d2, key1, key2;
  d1 = d2 = null;
  key1 = /foo/;
  key2 = /bar/;
  beforeEach(function() {
    d1 = TypedDictionary('RegExp', 'Number');
    d1.set(key1, 111);
    d1.set(key2, 222);
    return d2 = Dictionary({
      a: 1,
      b: 2,
      c: 3
    });
  });
  return describe('#remove', function() {
    it('should remove the key/val for the passed key', function() {
      d1.remove(key1);
      expect(d1.get(key1)).to.be(void 0);
      d2.remove('b');
      return expect(d2.get('b')).to.be(void 0);
    });
    it('should `true` if the key/val was successfully removed', function() {
      var x;
      x = d1.remove(key1);
      expect(x).to.be(true);
      x = d2.remove('b');
      return expect(x).to.be(true);
    });
    return it('should `false` if the key/val was not removed', function() {
      var x;
      x = d1.remove(/zzz/);
      expect(x).to.be(false);
      x = d2.remove('q');
      return expect(x).to.be(false);
    });
  });
});
